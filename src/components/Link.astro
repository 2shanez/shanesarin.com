---
import { cn } from "@lib/utils";

type Props = {
  href: string;
  external?: boolean;
  underline?: boolean;
}

const { href, external, underline = true, ...rest } = Astro.props;
---

<a 
  href={href} 
  target={ external ? "_blank" : "_self" } 
  class={cn("inline-block decoration-black/15 dark:decoration-white/30 hover:decoration-black/25 hover:dark:decoration-white/50 text-current hover:text-black hover:dark:text-white", underline && "underline underline-offset-2")}
  style="transition: color 0.3s cubic-bezier(0.16, 1, 0.3, 1), text-decoration-color 0.3s cubic-bezier(0.16, 1, 0.3, 1);"
  {...rest}>
  <slot/>
</a>
